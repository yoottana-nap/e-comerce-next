export const regexExpression = {
    regexEmail: new RegExp(/^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,4}$/i),
    regexText: new RegExp(/^[a-zA-Zก-๙]*$/),
    regexPassword: new RegExp(/^[a-zA-Z0-9]*$/),
    regexTel: new RegExp(/^\d+$/)
};
